# Git commands
```
git clone https://github.com/arjunachari12/github-sample-repo.git
git config --global user.name "arjunachari12"
git config --global user.email "arjun.achari12@gmail.com"
git status
git add .
git commit -m "fixed bug131"
git push -u origin main
```
